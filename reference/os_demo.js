const os = require('os');


//Platform
console.log(os.platform());

// gets the computer Cpu

console.log(os.arch());


// gets the cup 

console.log(os.cpus());

// gets the free memory
console.log(os.freemem());

// gets the total memory
console.log(os.totalmem());

// also can get the home dir
console.log(os.homedir());

// uptime
console.log(os.uptime());